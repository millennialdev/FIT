import { Container } from './alertStyles';

const Alert = () => {
  return (
    <Container>
      FREE Exclusive membership deal for one month when you take a tour (new
      members only). Terms apply. Learn More
    </Container>
  );
};

export default Alert;
